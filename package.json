{
  "name": "outlook-team-app",
  "version": "1.0.0",
  "description": "Outlook-like email application with Teams authentication - Frontend only, talks directly to Microsoft Graph API",
  "scripts": {
    "start": "cd client && npm start",
    "start:https": "cd client && npm run start:https",
    "build": "cd client && npm run build",
    "install-all": "cd client && npm install"
  },
  "keywords": ["outlook", "email", "teams", "microsoft-graph", "spa"],
  "author": "",
  "license": "MIT"
}

